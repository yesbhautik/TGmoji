# ================================================
# SVG → GIF & Telegram Converter — Configuration
# Copy this file to .env and adjust as needed.
# ================================================

# ── Server ──
PORT=3000
NODE_ENV=production
CORS_ORIGIN=*

# ── Browser Pool (Puppeteer) ──
MAX_CONCURRENT_BROWSERS=3      # Max simultaneous Chromium instances
MIN_BROWSERS=1                 # Pre-warmed browsers
BROWSER_IDLE_TIMEOUT_MS=60000  # Kill idle browsers after 60s

# ── Job Queue ──
MAX_QUEUE_SIZE=20               # Max pending jobs before rejecting (429)
JOB_TIMEOUT_MS=120000           # Abort job after 120s

# ── File Management ──
MAX_FILE_SIZE_MB=10
CLEANUP_INTERVAL_MIN=30         # Run cleanup every 30 min
OUTPUT_TTL_MIN=60               # Delete output files older than 60 min

# ── Rate Limiting ──
RATE_LIMIT_WINDOW_MS=900000     # 15 min window
RATE_LIMIT_MAX=100              # Max general requests per window per IP
CONVERT_RATE_LIMIT_MAX=10       # Max conversion requests per window per IP

# ── Docker / System Chromium ──
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
# PUPPETEER_SKIP_DOWNLOAD=true
